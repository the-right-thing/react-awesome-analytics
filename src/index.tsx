/** @jsx jsx */
import { jsx } from '@emotion/core';

const Button = () => (
    <div
        css={{
            color: 'red',
        }}
    >
        My button, oh gosh!
    </div>
);

export default Button;
